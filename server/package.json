{
  "dependencies": {
    "axios": "^0.19.2",
    "express": "^4.17.1",
    "jest": "^25.3.0",
    "jest-mock-axios": "^3.2.0",
    "pg": "^8.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.3"
  },
  "scripts": {
    "start": "nodemon index.js",
    "test": "jest",
    "start:db": "docker container run --name postgreshp -e POSTGRES_USER=hp -e POSTGRES_PASSWORD=secret -d -p 4000:1234 -v $(pwd)/my-postgres:/etc/postgresql/ postgres"
  }
}
